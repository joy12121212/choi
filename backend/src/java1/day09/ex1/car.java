package java1.day09.ex1;

public class car {
	//1.필드 : 객체의 데이터 저장하는 곳
	//2.생성자 : * 객체 생성시 초기화 역할 담당
		// 생성자 명은 클래스와 동일
		// 메소드와 비슷하지만 리턴 없다. 인수/매개값 있음
		// 생성자명 (매개변수 ,매개변수, 매개변수)
		// {실행코드}
		// 실행조건 : 1. 매개변수로 전달딘 값을 필드에 저장 2. 안전상 필드에 저장하기 전에 가공, 유효성 검사 진행
		// 필드는 실제 데이터 저장되는곳 --> 객체.필드 (권장x)
	car(String model , String color , int maxSpeed){}
	public car() { }
	
	
	
	
	//3.메소드
}
